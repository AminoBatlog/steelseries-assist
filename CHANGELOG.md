# 更新日志

## 0.0.4 - 2026-08-04

### 改进

- 音量与静音状态联动：0% 自动静音，恢复非零音量自动取消静音；
- 静音按钮将音量归零，再次点击恢复该通道最近一次非零音量；
- 支持单击音量轨道直接跳转到对应音量位置，并可保持左键继续连续拖动。

## 0.0.3 - 2026-08-01

### 新功能

- 接入 Sonar WebSocket，实时接收 GG、键盘媒体键和其他来源的音量与静音变化；
- Assist 调整 Game、Chat、Media、Aux、Mic 时改用对应 Windows Sonar 虚拟端点，使 GG 页面能够同步更新；
- WebSocket 断开后自动退避重连，并降级为音量专用 HTTP 轮询。

### 改进

- 滑块拖动期间按通道进行 75ms 节流，只保留最新待写入值，并在松开时提交最终值；
- 音量行改为稳定对象和增量更新，后台同步不再重建滑块或关闭设备下拉框；
- 设备、绑定和应用路由改为低频刷新，避免影响正在进行的交互；
- 自动测试扩展至 9 项，覆盖 WebSocket 部分事件解析、虚拟端点映射和写入合并。

### 已知限制

- Master 没有对应的 Windows 虚拟端点，Assist 修改 Master 时仍通过 HTTP 写入；已经打开的 GG 页面可能不会立即刷新该数值。

## 0.0.2 - 2026-08-01

### 改进

- 静音控制改为与整体深色界面一致的喇叭图标按钮；
- 静音状态使用喇叭加红色斜线、暗红背景和红色边框明确提示；
- 修复多尺寸 ICO 生成格式，使 Windows 资源管理器能够正确显示应用图标；
- 将应用图标显式嵌入可执行文件和 WPF 窗口资源。

## 0.0.1 - 2026-08-01

首个公开便携版本。

### 功能

- 常驻 Windows 系统托盘，单击图标快速显示或隐藏控制面板；
- 调整 Master、Game、Chat、Media、Aux、Mic 音量和静音状态；
- 切换各 Sonar 通道绑定的物理输入、输出设备；
- 按 Sonar 声卡分区展示活跃应用；
- 将应用拖放到 Game、Chat、Media 或 Aux，更新其默认输出；
- 正确展示 OBS 等同时连接多个 Sonar 音轨的应用；
- 面板可见时与 SteelSeries GG / Sonar 定时同步，并支持 Sonar 重启后重连。

### 已知限制

- 当前仅在 SteelSeries GG 116.0.0 上完成验证；
- 应用路由由 Sonar 按进程设置，部分程序需要重启或重新创建音频会话后才会完全切换；
- 第二次启动不会唤起已经运行的面板；
- 暂不支持多显示器任务栏定位、开机启动和安装器；
- 发布文件尚未进行代码签名，Windows 可能显示未知发布者提示。
