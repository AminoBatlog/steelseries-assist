# 当前开发状态

更新时间：2026-08-01

当前版本：0.0.2

## 已完成

- .NET 8 分层解决方案与 WPF 工程；
- Windows system tray 常驻和单实例保护；
- 点击托盘图标显示/隐藏轻量控制面板；
- 动态发现 `SteelSeriesSonar.exe` 的 loopback HTTP 服务；
- Sonar 在线状态、音量、设备、绑定和应用会话读取；
- Game、Chat、Media、Aux、Mic、Master 音量与静音控制；
- Game、Chat、Media、Aux 物理输出绑定；
- Mic 物理输入绑定；
- 按 Game、Chat、Media、Aux 声卡分区展示活跃播放应用；
- 支持将应用卡片拖到目标声卡，并通过 Sonar 按进程更新默认输出；
- 多会话应用会出现在所有实际连接的声卡分区中；
- 面板显示时每 2 秒回读 Sonar 状态，并在 Sonar 重启后重新发现连接；
- 所有音量写入后的状态回读确认；
- 非 loopback 地址拒绝策略；
- 脱敏控制台探针及原值写回验证模式；
- 零警告编译、4 项基础自动测试、GUI 启动冒烟测试。
- 正式托盘与可执行文件图标；
- Windows x64 自包含单文件发布配置。

## 当前限制

- 当前适配并验证的是 GG 116.0.0，尚未建立多版本兼容矩阵；
- GG 加密 `/subApps` 在本机触发 Windows SChannel 凭据错误，目前实际使用 TCP owner table 发现策略；
- 第二次启动只会被单实例保护拦截，尚不会唤起已经运行的面板；
- 弹窗目前按主显示器工作区定位，多显示器任务栏定位尚未实现；
- 拖放路由按进程执行；OBS 等已打开的多音轨会话会继续在多个声卡分区中显示，部分应用需重启或重新创建音频会话后才完全采用新的默认输出；
- 尚未接入 Sonar 的事件/WebSocket 推送，目前使用面板可见期间的 2 秒轮询同步；
- 尚未加入开机启动、日志、设置页、场景和安装包；
- 发布文件尚未进行代码签名，Windows 可能提示未知发布者；
- 当前提供便携压缩包，尚无安装器。

## 下一步

1. 增加命名管道，使第二次启动可以唤起现有面板；
2. 研究 Sonar 事件/WebSocket 接口，以事件推送替代定时状态同步；
3. 优化多显示器、DPI、任务栏定位和失焦行为；
4. 增加设置页、开机启动和结构化日志；
5. 建立 GG 版本能力探测与未知版本安全模式；
6. 增加代码签名和安装包。
